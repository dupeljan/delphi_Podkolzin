unit Unit2;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.Menus, Vcl.StdCtrls;

type
  TMAIN = class(TForm)
    RESOURCES: TButton;
    PURCHASE_INV: TButton;
    PRODUCTS: TButton;
    DAYLY_INCOME: TButton;
    LOSS: TButton;
    Label1: TLabel;
    procedure RESOURCESClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  MAIN: TMAIN;

implementation

{$R *.dfm}

uses Unit3;

procedure TMAIN.RESOURCESClick(Sender: TObject);
begin
  Form3.ShowModal;
end;

end.
